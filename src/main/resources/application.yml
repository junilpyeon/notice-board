spring:
  profiles:
    active: local
  cache:
    type: none

logging:
  level:
    root: INFO
    com.noticeboard: DEBUG

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    try-it-out-enabled: true